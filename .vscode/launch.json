{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "name": "Launch Miel++",
            "addonPath": "${workspaceFolder}"
        }
    ]
}